<!DOCTYPE html>
<html>
  <head>
    <title>CryptoWallet</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag params[:controller], "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <%= javascript_importmap_tags %>
    <%= javascript_include_tag params[:controller], "data-turbolinks-track": "reload" %>
  </head>

  <body>
    <div class="container-fluid">
      <ul class="nav justify-content-end mb-3">
        <%= link_to t("links.home"), root_path, class:"btn btn-outline-primary position-absolute start-0 ms-3 mt-2" %>
        <li class="nav-item">
          <%= link_to "Português", params.merge(locale: "pt-BR").permit(:locale), class:"nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "English", params.merge(locale: "en").permit(:locale), class:"nav-link" %>
        </li>
      </ul>

      <%= yield %>
    </div>
  </body>
</html>