<p style="color: green"><%= notice %></p>

<table>
    <thead>
      <tr>
        <th>Descrição</th>
        <th>Sigla</th>
        <th>Imagem</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <tr>
        <%= render @coin %>
      </tr>
    </tbody>
</table>

<div>
  <%= link_to t("links.edit"), edit_coin_path(@coin) %> |
  <%= link_to t("links.back"), coins_path %>
  <p> <%= button_to t("links.delete"), @coin, method: :delete %> </p>
</div>
